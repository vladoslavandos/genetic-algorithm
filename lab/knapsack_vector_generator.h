#ifndef __KNAPSACK_VECTOR_GENERATOR_H_INCLUDED__
#define __KNAPSACK_VECTOR_GENERATOR_H_INCLUDED__
#include <bits/stdc++.h>

using namespace std;

// Константы
constexpr int MAX[] = {
  (int)pow(2, 24/0.8),
  (int)pow(2, 24/0.9),
  (int)pow(2, 24/1.0),
  (int)pow(2, 24/1.1),
  (int)pow(2, 24/1.2),
  (int)pow(2, 24/1.3),
  (int)pow(2, 24/1.4)
}; 
constexpr int SIZE = 24;
constexpr int NUMBER_OF_VECTORS = 7;

#endif
